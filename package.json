{
  "name": "pipefy-stream",
  "version": "1.0.0",
  "description": "A nodeJS utility to handle streams pipeline",
  "main": "src/index.js",
  "scripts": {
    "test": "tap -b test/*.test.js --100",
    "lint": "prettier --write '{src,test}/*.js' && eslint --fix '{src,test}/*.js'"
  },
  "keywords": [
    "streams",
    "pipeline"
  ],
  "author": "Flavio Lucio",
  "license": "MIT",
  "devDependencies": {
    "eslint": "5.13.0",
    "prettier": "1.16.4",
    "tap": "12.1.1"
  }
}
